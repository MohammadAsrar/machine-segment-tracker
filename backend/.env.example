# Server Settings
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=your-mongodb-connection-uri-here
DB_NAME=patternlabDB

# CORS Settings
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info

# JWT (Optional – if you implement authentication later)
JWT_SECRET=your-secret-key
TOKEN_EXPIRES_IN=1d

# Debugging (Optional)
DEBUG=true
